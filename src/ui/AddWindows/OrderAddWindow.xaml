<Window x:Class="store_accounting_system.ui.OrderAddWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Order" Height="520" Width="700"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Row 0: CustomerId + Date -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,12">
            <StackPanel Width="200" Margin="0,0,12,0">
                <TextBlock Text="CustomerId"/>
                <TextBox x:Name="TbCustomerId" />
            </StackPanel>
        </StackPanel>

        <!-- Row 1: кнопки управления строками -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Add Product" Width="160" Margin="0,0,8,0" Click="BtnAddItem_Click"/>
            <Button Content="Delete selected" Width="160" Click="BtnRemoveItem_Click"/>
        </StackPanel>

        <!-- Row 2: таблица OrderItems -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding OrderItems}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="False"
                  SelectionMode="Single"
                  x:Name="GridOrderItems"
                  Margin="0,0,0,12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ProductId" Binding="{Binding ProductId, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Quantity"  Binding="{Binding Quantity,  UpdateSourceTrigger=PropertyChanged}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Row 3: OK/Cancel -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Add" Width="120" Margin="0,0,8,0" Click="BtnAdd_Click"/>
            <Button Content="Cancel" Width="120" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
